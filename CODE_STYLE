# isrvd 代码规范

## 整体要求

- 确保每个模块或包都符合高内聚、低耦合设计特性
- 确保代码中变量命名、方法签名、实现逻辑等一致性
- 所有代码提交必须通过 lint 检查，不引入新的 warning

---

## 前端规范（webview/src）

### 卡片结构

所有列表页/详情页使用 `.card mb-4` 包裹，标题栏容器统一使用：

```
bg-slate-50 border-b border-slate-200 rounded-t-2xl px-4 md:px-6 py-3
```

- 移动端左右内边距 `px-4`（16px），桌面端 `md:px-6`（24px）
- 容器本身 **不写** `flex items-center justify-between`，由下面桌面/移动两个子 div 各自 `hidden md:flex` / `flex md:hidden` 控制布局
- 所有标题栏必须同时提供桌面版 + 移动版两套子 div（详见下方「列表页标题栏双布局」与「详情页标题栏」）

### 列表页标题栏双布局

```html
<div class="bg-slate-50 border-b border-slate-200 rounded-t-2xl px-4 md:px-6 py-3">
  <!-- 桌面端 -->
  <div class="hidden md:flex items-center justify-between">
    <div class="flex items-center gap-3">
      <div class="w-9 h-9 rounded-lg bg-{模块主色}-500 flex items-center justify-center">
        <i class="fas fa-{icon} text-white"></i>
      </div>
      <div>
        <h1 class="text-lg font-semibold text-slate-800">页面标题</h1>
        <p class="text-xs text-slate-500">页面描述</p>
      </div>
    </div>
    <div class="flex items-center gap-2">
      <button>刷新</button>
      <button>创建</button>
    </div>
  </div>
  <!-- 移动端 -->
  <div class="flex md:hidden items-center justify-between gap-2">
    <div class="flex items-center gap-3 min-w-0 flex-1">
      <div class="w-9 h-9 rounded-lg bg-{模块主色}-500 flex items-center justify-center flex-shrink-0">
        <i class="fas fa-{icon} text-white"></i>
      </div>
      <div class="min-w-0">
        <h1 class="text-lg font-semibold text-slate-800 truncate">页面标题</h1>
      </div>
    </div>
    <div class="flex items-center gap-1 flex-shrink-0">
      <!-- 9x9 图标按钮（刷新/创建等） -->
    </div>
  </div>
</div>
```

移动端要点：外层 `gap-2` 避免文字组挤压按钮；文字组必须 `min-w-0 flex-1`、标题加 `truncate`；按钮组 `flex-shrink-0`。

### 详情页标题栏

详情页（`xxx/image.vue`、`network.vue`、`volume.vue`、`swarm/node.vue`、`swarm/service-*.vue`、`widget/container-nav.vue` 等）与列表页使用相同的标题栏结构，右侧仅保留刷新等功能按钮。**不添加返回按钮**，用户通过侧边栏菜单导航即可。

### 标题图标

```
w-9 h-9 rounded-lg bg-{模块主色}-500 flex items-center justify-center
```

图标用 `text-white`。

### 主操作按钮（创建/拉取等）

颜色必须与本页标题图标的模块主色保持一致：

```
px-3 py-1.5 rounded-lg bg-{模块主色}-500 hover:bg-{模块主色}-600 text-white text-xs font-medium flex items-center gap-1.5 transition-colors
```

### 刷新按钮（统一）

```
px-3 py-1.5 rounded-lg bg-white border border-slate-200 hover:bg-slate-50 text-slate-700 text-xs font-medium flex items-center gap-1.5 transition-colors
```

### Toolbar 按钮区

统一用单层 `<div class="flex items-center gap-2">`，**禁止**在内部再嵌套额外的 `<div class="flex items-center gap-2 ml-2">`：

```html
<!-- ✅ 正确 -->
<div class="flex items-center gap-2">
  <button>刷新</button>
  <button>创建</button>
</div>

<!-- ❌ 错误 -->
<div class="flex items-center gap-2">
  <div class="flex items-center gap-1 bg-slate-100 p-1 rounded-lg">...</div>
  <div class="flex items-center gap-2 ml-2">  <!-- 多余嵌套 -->
    <button>刷新</button>
    <button>创建</button>
  </div>
</div>
```

### Tab 切换组件

同一页面内多个子视图切换统一使用 **卡片头部 tab 组**（容器类顶部 toolbar 已有示例），不在内容区单独做 tab 条：

```html
<!-- 桡面端：toolbar 右侧 -->
<div class="flex gap-1 bg-slate-100 p-1 rounded-lg">
  <button
    v-for="tab in tabs"
    :key="tab.key"
    @click="activeTab = tab.key"
    :class="[
      'px-3 py-1 text-xs font-medium rounded-md transition-all duration-200 flex items-center gap-1.5',
      activeTab === tab.key ? 'bg-white text-blue-600 shadow-sm' : 'text-slate-500 hover:text-slate-700'
    ]"
  >
    <i :class="['fas', tab.icon]"></i><span>{{ tab.label }}</span>
  </button>
</div>

<!-- 移动端：toolbar 下方独立一行，同样的胶囊式容器，justify-center -->
<div class="flex justify-center gap-1 bg-slate-100 p-1 rounded-lg">
  <!-- 同上 -->
</div>
```

> 标题所属的描述文案按 `activeTab` 动态切换，不要写死一句总述。Tab 数量多于 3 个时优先拆为独立路由（见 Settings 模块化规范）。

### 表单布局

表单容器统一 `max-w-3xl space-y-4`；每个字段三段式：`<label>` + `<input class="input">` + `<p class="mt-1 text-xs text-slate-400">说明</p>`。不要使用自定义的 input 样式，统一使用 `.input` 类。

### 保存按钮 + 说明文字的响应式布局

表单底部的保存按钮常需搭配一段说明文字（如“重启后生效”等），窄屏下需防止按钮被按压换行：

```html
<!-- ✅ 正确 -->
<div class="pt-2 flex flex-col sm:flex-row sm:items-center gap-3">
  <button type="submit" :disabled="saving"
          class="self-start px-4 py-2 rounded-lg bg-blue-500 hover:bg-blue-600 disabled:opacity-50 text-white text-xs font-medium flex items-center gap-1.5 transition-colors whitespace-nowrap flex-shrink-0">
    <i :class="['fas', saving ? 'fa-spinner fa-spin' : 'fa-save']"></i>{{ saving ? '保存中...' : '保存配置' }}
  </button>
  <p class="text-xs text-slate-400 flex items-start gap-1">
    <i class="fas fa-circle-info mt-0.5"></i>
    <span>说明文字，可能很长。</span>
  </p>
</div>
```

要点：
- 容器：`flex flex-col sm:flex-row sm:items-center gap-3` — 窄屏竖向堆叠，`sm` 以上水平
- 按钮：必须加 `whitespace-nowrap flex-shrink-0 self-start` — 文字不换行、不被压缩、竖向时不拉伸全宽
- 说明：`flex items-start`，图标加 `mt-0.5` 对齐首行，文字用 `<span>` 包裹避免分行错位

### 敏感字段（密码/密钥）回显规范

后端返回配置时 **绝不回流明文密码/密钥**，改为同时返回一个布尔标志位（如 `jwtSecretSet`、`adminKeySet`）表示是否已设置。前端按以下规则展示与提交：

- 表单字段统一 `type="password"` + `autocomplete="new-password"`，v-model 绑定的字段加载后置空，仅用标志驱动 placeholder：
  - 已设置：`已设置（留空保持不变）`
  - 未设置：`尚未设置`
- label 旁加状态徽标：`bg-green-50 text-green-700` 已设置 / `bg-slate-100 text-slate-500` 未设置
- 保存时 **留空即保留原值**，后端应跳过空字段，避免覆盖掉已有密钥

```html
<label class="block text-sm font-medium text-slate-700 mb-2">
  JWT 密钥
  <span v-if="jwtSecretSet" class="ml-1 inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-medium bg-green-50 text-green-700">
    <i class="fas fa-check mr-0.5"></i>已设置
  </span>
  <span v-else class="ml-1 inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-medium bg-slate-100 text-slate-500">未设置</span>
</label>
<input type="password" v-model="form.jwtSecret" :placeholder="jwtSecretPlaceholder" class="input" autocomplete="new-password" />
```

### Settings 模块化

`/system/settings` 和 `/system/members` 拆为独立路由与页面，而非单页多 tab：

- `webview/src/views/system/settings.vue` — 服务器 / Docker / APISIX 基础配置（内部 3 tab）
- `webview/src/views/system/members.vue` — 成员（用户）管理，从系统设置中拆出，作为独立主导航
- 侧边栏菜单中“系统设置”项统一排在 **最后**
- 系统账号（用户列表中 `id === 1` 或第一个帐号）**禁止删除**，按钮置灰并使用 `title` 提示“系统账号不允许删除”

路由示例：

```js
{ path: '/system/members', component: () => import('@/views/system/members.vue') }
{ path: '/system/settings', component: () => import('@/views/system/settings.vue') }
{ path: '/docker/registries', component: () => import('@/views/docker/registries.vue') }
```

### 自动刷新间隔

全局统一使用 `POLL_INTERVAL` 常量，定义在 `webview/src/helper/utils.js`（当前值 `3000` ms）。所有 `setInterval` 轮询定时器必须从 `utils.js` 导入此常量，**禁止**在组件内硬编码刷新间隔数字：

```js
// ✅ 正确
import { POLL_INTERVAL } from '@/helper/utils'
this.timer = setInterval(() => this.loadData(), POLL_INTERVAL)

// ❌ 错误
this.timer = setInterval(() => this.loadData(), 3000)
```

### 响应式布局规范

所有列表页统一采用**双视图**结构：桌面端表格 + 移动端卡片。

#### 双视图结构

```html
<!-- 桌面端表格 -->
<div class="hidden md:block overflow-x-auto">
  <table>...</table>
</div>

<!-- 移动端卡片列表 -->
<div class="md:hidden space-y-3 p-4">
  <div v-for="item in list" class="rounded-xl border border-slate-200 bg-white p-4 transition-all hover:shadow-sm">
    ...
  </div>
</div>
```

> **注意**：移动端卡片列表外层 div **必须**使用 `md:hidden space-y-3 p-4`，**不得省略 `p-4`**。列表被 `.card` 包裹时，外层卡片的圆角背景会紧贴子卡片边缘，`p-4` 提供的 16px 内边距用于与卡片头部、左右两侧之间形成呼吸空间；`space-y-3` 仅控制子卡片之间的垂直间距，无法替代外层 padding。间距模型：`.card` → `md:hidden space-y-3 p-4`（16px 四边留白）→ 子卡片 `rounded-xl border p-4`（12px 卡片间距、16px 卡片内边距）。

#### 移动端卡片规范

- 卡片容器：`rounded-xl border border-slate-200 bg-white p-4 transition-all hover:shadow-sm`
- 卡片图标：`w-10 h-10 rounded-lg`（比桌面端 `w-8 h-8` 大一号，适合触摸操作）
- 操作按钮区：`flex flex-wrap gap-1 pt-2 border-t border-slate-100`

#### 搜索框规范

有搜索功能的页面，搜索框需同时提供桌面端和移动端两个入口：

```html
<!-- 桌面端搜索框（在 toolbar 内） -->
<div class="relative hidden md:block">
  <input v-model="searchText" type="text" placeholder="搜索..." class="pl-8 pr-3 py-1.5 text-xs border border-slate-200 rounded-lg ... w-48" />
  <i class="fas fa-search absolute left-2.5 top-1/2 -translate-y-1/2 text-slate-400 text-xs"></i>
</div>

<!-- 移动端搜索框（在 toolbar 下方单独一行） -->
<div class="md:hidden px-4 py-2 border-b border-slate-100">
  <div class="relative">
    <input v-model="searchText" type="text" placeholder="搜索..." class="w-full pl-8 pr-3 py-1.5 text-xs border border-slate-200 rounded-lg ..." />
    <i class="fas fa-search absolute left-2.5 top-1/2 -translate-y-1/2 text-slate-400 text-xs"></i>
  </div>
</div>
```

> 移动端搜索框宽度必须为 `w-full`，桌面端固定 `w-48`。两者共用同一个 `v-model`，无需额外逻辑。

### 表格

| 区域 | 样式 |
|------|------|
| thead | `bg-slate-50 border-b border-slate-200` |
| 表头文字 | `text-xs font-semibold text-slate-600 uppercase tracking-wider` |
| tbody | `bg-white divide-y divide-slate-100` |
| 行 hover | `hover:bg-slate-50 transition-colors` |

### Loading 状态

```html
<div class="flex flex-col items-center justify-center py-20">
  <div class="w-12 h-12 spinner mb-3"></div>
  <p class="text-slate-500">加载中...</p>
</div>
```

### 空状态

```html
<div class="flex flex-col items-center justify-center py-20">
  <div class="w-20 h-20 rounded-full bg-slate-100 flex items-center justify-center mb-4">
    <i class="fas fa-{icon} text-4xl text-slate-300"></i>
  </div>
  <p class="text-slate-600 font-medium mb-1">暂无数据</p>
</div>
```

### 操作按钮语义色

| 操作 | 样式 |
|------|------|
| 查看日志/详情 | `text-slate-600 hover:bg-slate-100` |
| 重启/重部署 | `text-blue-600 hover:bg-blue-50` |
| 启动/激活 | `text-emerald-600 hover:bg-emerald-50` |
| 停止/警告 | `text-amber-600 hover:bg-amber-50` |
| 删除 | `text-red-600 hover:bg-red-50` |
| 网络相关/扩缩容 | `text-indigo-600 hover:bg-indigo-50` |
| 暂停/查看详情等中性操作 | `text-slate-600 hover:bg-slate-50` |

### 样式一致性要求

所有页面必须保持以下样式一致性：

#### 表格表头样式
- 所有表格表头必须使用统一的 `text-xs font-semibold text-slate-600 uppercase tracking-wider` 样式
- 检查范围：Docker、Swarm、APISIX、System 等所有模块

#### 卡片标题栏样式  
- 所有页面必须使用统一的 `bg-slate-50 border-b border-slate-200 rounded-t-2xl px-4 md:px-6 py-3` 样式
- 23个文件都必须遵循此规范

#### 移动端卡片布局
- 所有页面必须使用统一的 `md:hidden space-y-3 p-4` 样式
- 13个主要列表页面都必须正确实现

#### 按钮颜色语义系统
所有操作按钮都必须遵循统一的颜色语义规范：
- 🟢 **绿色/翠绿色**：创建/启动/激活操作（`text-emerald-600 hover:bg-emerald-50`）
- 🔵 **蓝色**：编辑/重启/重部署操作（`text-blue-600 hover:bg-blue-50`）  
- 🟣 **紫色**：网络相关/扩缩容操作（`text-indigo-600 hover:bg-indigo-50`）
- 🟠 **琥珀色**：停止/排空/警告操作（`text-amber-600 hover:bg-amber-50`）
- 🔴 **红色**：删除操作（`text-red-600 hover:bg-red-50`）
- ⚪ **灰色**：暂停/查看详情等中性操作（`text-slate-600 hover:bg-slate-50`）

#### 按钮类名统一
- 所有按钮都必须使用统一的 `btn-icon` 类名
- 50个按钮实例都必须遵循此规范

#### 按钮文本一致性
- 所有按钮的文本和功能必须正确对应，避免出现"激活"按钮提示"暂停"等问题
- 按钮状态必须与操作语义一致，如激活按钮对应激活状态，暂停按钮对应暂停状态

### confirm 弹窗

icon 参数只传图标名（不含 `fas` 前缀），如 `icon: 'fa-trash'`，颜色用语义词如 `iconColor: 'red'`。

### 导航折叠状态

侧边栏折叠时，Docker 和 Swarm 的图标链接统一跳转到 `/overview`（整体概览页），不跳转各自的子路由。

### 路由结构

- `/overview`：整体概览（引入 docker/overview 和 swarm/overview 组件）
- docker/overview 和 swarm/overview 仅作为组件使用，不提供独立路由入口和菜单项

### Settings 模块化规范

`/system/settings` 和 `/system/members` 必须拆为独立路由与页面，而非单页多 tab：

- `webview/src/views/system/settings.vue` — 服务器 / Docker / APISIX 基础配置（内部 3 tab）
- `webview/src/views/system/members.vue` — 成员（用户）管理，从系统设置中拆出，作为独立主导航
- 侧边栏菜单中"系统设置"项统一排在 **最后**
- 系统账号（用户列表中 `id === 1` 或第一个帐号）**禁止删除**，按钮置灰并使用 `title` 提示"系统账号不允许删除"

路由示例：

```js
{ path: '/system/members', component: () => import('@/views/system/members.vue') }
{ path: '/system/settings', component: () => import('@/views/system/settings.vue') }
{ path: '/docker/registries', component: () => import('@/views/docker/registries.vue') }
```

导航链接和 instructions.ts 路径匹配均使用这两个路径：`/system/members`（成员管理）、`/system/settings`（系统设置）

### 工具函数

通用工具函数统一放在 `webview/src/helper/utils.js` 中：

- 文件大小格式化统一使用 `formatFileSize`
- Apisix upstream 节点解析统一使用 `parseUpstreamNode`
- Apisix 路由 payload 构建统一使用 `buildRoutePayload`
- 时间格式化统一使用 `formatTime`
- 文件下载统一使用 `downloadFile`
- 文件图标获取统一使用 `getFileIcon`
- 可编辑文件判断统一使用 `isEditableFile`
- 禁止在页面组件中重复定义与 utils.js 功能相同的函数

### 终端组件

终端（Shell/Container Exec）相关功能统一放在 `webview/src/helper/` 下：

- 系统终端统一使用 `shell.js` 中的 `create` 和 `destroy`
- 容器终端统一使用 `container-exec.js` 中的 `create` 和 `destroy`
- 终端配置统一：`{ theme: { background: '#222' }, fontSize: 15, cursorBlink: true }`
- WebSocket 协议：根据页面协议自动选择 `ws://` 或 `wss://`
- 终端适配统一使用 `@xterm/addon-fit`，窗口 resize 时自动 fit
- 禁止在页面组件中直接创建 Terminal 实例或 WebSocket 连接

---

## 后端规范（internal/）

### HTTP 状态码

使用 Go 标准库常量，禁止硬编码数字：

```go
// ✅ 正确
helper.RespondSuccess(c, "登录成功", resp)
helper.RespondError(c, http.StatusUnauthorized, "未提供认证令牌")
helper.RespondError(c, http.StatusForbidden, "权限不足")
helper.RespondError(c, http.StatusBadRequest, "参数错误")
helper.RespondError(c, http.StatusInternalServerError, "内部错误")
helper.RespondError(c, http.StatusNotFound, "资源不存在")

// ❌ 错误
c.JSON(401, gin.H{"error": "未提供认证令牌"})
c.JSON(403, gin.H{"error": "权限不足"})
```

### 成功响应

统一使用 `helper.RespondSuccess` 返回成功，避免在 handler 中直接拼接 `gin.H`：

```go
// ✅ 正确
helper.RespondSuccess(c, "操作成功", nil)
helper.RespondSuccess(c, "创建成功", gin.H{"id": "123"})

// ❌ 错误
c.JSON(http.StatusOK, gin.H{"success": true, "message": "操作成功"})
```

### 错误响应

统一使用 `helper.RespondError` 返回错误，避免在 handler 中直接拼接 `gin.H`：

```go
// ✅ 正确
helper.RespondError(c, http.StatusBadRequest, "参数错误")

// ❌ 错误
c.JSON(http.StatusBadRequest, gin.H{"error": "参数错误"})
```

### 参数校验

使用 Gin 的 `ShouldBindJSON`（或 `ShouldBindQuery`、`ShouldBindURI`）进行请求参数绑定和校验：

```go
// ✅ 正确 — 使用 ShouldBindJSON，失败时透传 err.Error()
var req model.ContainerActionRequest
if err := c.ShouldBindJSON(&req); err != nil {
    helper.RespondError(c, http.StatusBadRequest, err.Error())
    return
}

// ❌ 错误 — 手动解析 JSON
var body map[string]any
if err := c.BindJSON(&body); err != nil { ... }
id := body["id"].(string) // 不安全的类型断言

// ❌ 错误 — 丢弃具体错误信息，使用固定字符串
if err := c.ShouldBindJSON(&req); err != nil {
    helper.RespondError(c, http.StatusBadRequest, "无效的JSON") // 丢失字段级错误
    return
}
```

请求模型统一在各 handler 子包中按需定义，可内联在 `handler.go` 中，也可单独放在 `model.go`，绑定标签使用 `binding:"required"` 等验证规则。业务数据模型定义在对应的 `pkgs/` 子包中，**struct 就近放置在使用它的第一个方法上方**。

### WebSocket 升级器

全局统一使用 `helper.WsUpgrader`，禁止在 handler 中定义本地 upgrader：

```go
// ✅ 正确
conn, err := helper.WsUpgrader.Upgrade(c.Writer, c.Request, nil)

// ❌ 错误
var upgrader = websocket.Upgrader{CheckOrigin: func(r *http.Request) bool { return true }}
conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
```

### 类型定义

类型定义按职责分层：

- **请求/响应结构体**：定义在各 handler 子包中，按需放在 `model.go`（如 `internal/handler/apisix/model.go`、`internal/handler/filer/model.go`）或内联在 `handler.go`（如 `internal/handler/system/handler.go`）
- **业务数据模型**：定义在 `pkgs/` 子包中，**struct 应就近放置在使用它的第一个方法上方**，禁止集中放在单独的 `types.go` 文件中
- **配置类型**：定义在 `config/types.go` 中

**pkgs 层 struct 放置规范**（参照 apisix 包）：

```go
// ✅ 正确 — struct 放在使用它的第一个方法上方
// ContainerInfo 放在 ListContainers 上方
type ContainerInfo struct {
    ID     string
    Names  []string
    Image  string
    Status string
}

func (s *DockerService) ListContainers(ctx context.Context, all bool) ([]*ContainerInfo, error) {
    // ...
}

// ContainerActionRequest 放在 ContainerAction 上方
type ContainerActionRequest struct {
    ID     string `json:"id" binding:"required"`
    Action string `json:"action" binding:"required"`
}

func (s *DockerService) ContainerAction(ctx context.Context, req *ContainerActionRequest) error {
    // ...
}
```

禁止在不同包中重复定义相同结构体。handler 层可直接引用 pkgs 层的请求类型（如 `docker.ContainerCreateRequest`）。

### Go 命名规范

缩写词统一全大写（如 CPU、ID、URL、HTTP 等），禁止大小写混合：

```go
// ✅ 正确
type ContainerInfo struct {
    CPUCores    float64 `json:"cpuCores"`
    ContainerID string  `json:"containerID"`
    APIVersion  string  `json:"apiVersion"`
}

// ❌ 错误
type ContainerInfo struct {
    CpuCores    float64 `json:"cpuCores"`    // 应为 CPUCores
    ContainerId string  `json:"containerId"` // 应为 ContainerID
    ApiVersion  string  `json:"apiVersion"`  // 应为 APIVersion
}
```

### fmt.Errorf 规范

禁止将变量字符串直接作为格式字符串传入，必须使用格式化占位符，避免格式化注入风险：

```go
// ✅ 正确
return nil, fmt.Errorf("create container failed: %w", err)
return nil, fmt.Errorf("service %s not found", name)

// ❌ 错误 — 变量字符串可能包含 % 导致意外格式化
return nil, fmt.Errorf(err.Error())
return nil, fmt.Errorf(msg)
```

### 日志规范

统一使用 `github.com/rehiy/pango/logman` 进行日志记录，禁止使用标准库 `log` 或 `fmt.Println`：

```go
// ✅ 正确
logman.Info("User logged in", "username", username)
logman.Warn("Terminal access denied", "username", username)
logman.Error("WebSocket 升级错误", "error", err)

// ❌ 错误
log.Println("User logged in:", username)
fmt.Printf("Error: %v\n", err)
```

日志级别使用规范：

| 级别 | 使用场景 |
|------|----------|
| `logman.Info` | 正常业务流程（登录、创建、删除等） |
| `logman.Warn` | 非致命异常（权限不足、降级操作等） |
| `logman.Error` | 错误与异常（操作失败、连接错误等） |

日志键值对格式：`"key", value`，禁止拼接字符串：

```go
// ✅ 正确
logman.Info("Container created", "name", req.Name, "id", resp.ID)

// ❌ 错误
logman.Info("Container created: name=" + req.Name + ", id=" + resp.ID)
```

### 工具函数
工具函数按作用域分层：

- **跨包通用工具**：放在 `internal/helper/` 包中（如 `response.go`、`websocket.go`）
- **业务包内私有工具**：放在各 `pkgs/` 子包的 `helper.go` 中（如 `pkgs/docker/helper.go`），设为包内私有
- **handler 包内私有工具**：放在各 handler 子包的 `helper.go` 中（如 `internal/handler/filer/helper.go`），设为包内私有

禁止在不同包中重复定义功能相同的函数。

### 中间件规范

中间件在 `internal/server/` 包内定义，不使用独立的 `middleware` 包：

- 认证中间件：`server.AuthMiddleware()` — 定义在 `internal/server/auth.go`，自动根据配置选择 JWT 或代理 Header 认证
- CORS 和 Recovery：由 `pango/httpd` 框架内置处理（`httpd.Engine` 已包含），无需单独定义中间件函数

```go
// ✅ 正确 — 认证中间件直接在路由中使用
authGroup.Use(server.AuthMiddleware())

// ❌ 错误 — 使用不存在的独立 middleware 包
authGroup.Use(middleware.AuthMiddleware())
```

### 服务层规范

业务逻辑分两层实现：

- **`pkgs/` 层**：纯业务逻辑，返回原生类型（如 Docker SDK 类型、Apisix 原始结构），不感知 HTTP 框架
- **`internal/service/` 层**：负责类型转换、业务组合、参数校验等，不感知 HTTP 框架
- **`internal/server/handler_*.go`**：轻量 handler，只负责解析请求、调用 service、返回响应

```go
// ✅ 正确 — handler 调用 service，service 调用 pkgs
// internal/server/handler_docker.go
func (app *App) dockerListContainers(c *gin.Context) {
    result, err := app.dockerSvc.ListContainers(c.Request.Context())
    if err != nil {
        helper.RespondError(c, http.StatusInternalServerError, err.Error())
        return
    }
    helper.RespondSuccess(c, "ok", result)
}
```

简单业务（如 auth）直接在 handler 内实现，不需要独立 service 层。

**依赖方向**：
- `pkgs/` 层禁止依赖 `internal/` 包（handler、helper、server 等）
- `internal/registry` 层负责从 `config` 读取配置并初始化 `pkgs/` 层服务
- `internal/service/` 层从 `internal/registry` 获取已初始化的 pkgs 实例，处理业务逻辑
- `internal/server/` 层负责路由注册、中间件配置，handler 调用 service

```
依赖流向：config → internal/registry → pkgs/
                    ↓
                 internal/service/
                    ↓
                 internal/server/
```

### 服务注册中心规范

所有外部服务（Docker、Apisix 等）的连接统一管理在 `internal/registry/` 包中，禁止 service/handler 直接从 `pkgs/` 或 `config` 创建服务实例：

```go
// ✅ 正确 — service 从 registry 获取已初始化的 pkgs 实例
func NewDockerService() (*DockerService, error) {
    svc := registry.DockerService
    if svc == nil {
        return nil, fmt.Errorf("Docker 服务未初始化")
    }
    return &DockerService{client: svc}, nil
}

// ❌ 错误 — 直接创建 pkgs 实例
func NewDockerService() (*DockerService, error) {
    cfg := &docker.DockerConfig{
        Host: config.Docker.Host,  // 直接依赖 config
    }
    svc, err := docker.NewDockerService(cfg)
    // ...
}
```

**启动流程**：`main → config.Load → registry.Init → server.NewApp`

**设计原则**：
- `internal/registry` 负责所有外部服务的生命周期管理（初始化、连接复用、可用性检查）
- `internal/service/` 从 registry 获取 pkgs 实例，处理业务逻辑与类型转换
- `internal/server/` handler 调用 service，只处理 HTTP 层逻辑
- 服务初始化失败不应阻止应用启动（可选服务），service 中通过 nil 检查处理未配置的情况

### 服务注册与依赖注入规范（internal/registry/）

- registry.go（Init 函数）、docker.go、apisix.go、compose.go
- 全局实例：registry.DockerService/SwarmManager/ApisixClient
- 可用性检查：IsDockerAvailable/IsSwarmAvailable/IsApisixAvailable
- Service 通过 registry 获取实例；启动流程：main → config.Load → registry.Init → server.NewApp

### 应用入口（internal/server/）

- app.go：路由注册与应用启动（NewApp 函数）
- auth.go：认证中间件（AuthMiddleware，JWT / 代理 Header）
- handler_*.go：轻量 handler，只负责解析请求、调用 service、返回响应
- 无独立 middleware 包，CORS/Recovery 由 pango/httpd 框架内置处理

### pkgs 包规范

- 禁止依赖 internal/ 包，配置通过构造函数注入
- pkgs 内部子包可互相引用（如 swarm 引用 docker 的 ParseDockerLogs），避免循环依赖
- **返回原生类型**：pkgs 层方法尽可能返回 SDK 原生类型，不做业务层的类型转换
- **错误透传原则**：pkgs 层调用外部 SDK/API 失败时，直接透传原始错误，不套操作前缀；仅在 pkgs 层自身逻辑（如 JSON 解析）失败时才添加描述前缀
- docker/：client.go+container.go+image.go+network.go+volume.go+stats.go+compose.go+exec.go+registry.go+helper.go（无 types.go）
- swarm/：client.go+node.go+service.go+task.go（无 types.go，类型分散到各文件）
- apisix/：client.go+route.go+consumer.go+upstream.go+plugin.go+plugin_config.go
- archive/：zip.go；auth 直接在 handler 内实现

```go
// ✅ 正确 — pkgs 层直接透传 SDK 错误
func (c *Client) ListRoutes() ([]Route, error) {
    data, err := c.doRequest(http.MethodGet, "/routes", nil)
    if err != nil {
        return nil, err  // 直接透传，不套 "获取路由失败: %w"
    }
    // ...
}

// ✅ 正确 — pkgs 层自身逻辑失败时才加描述
if err := json.Unmarshal(data, &raw); err != nil {
    return nil, fmt.Errorf("解析路由详情失败: %w", err)
}

// ❌ 错误 — 套操作前缀导致多层嵌套
if err != nil {
    return nil, fmt.Errorf("获取路由失败: %w", err)  // 最终错误变成 "获取路由失败: 请求 Apisix 失败: ..."
}
```

### 项目结构

```
internal/
├── service/         # 业务逻辑层（类型转换、业务组合、参数校验，不感知 HTTP 框架）
│   ├── apisix/          # Apisix 业务逻辑
│   │   └── service.go
│   ├── compose/         # Compose 业务逻辑
│   │   ├── service.go
│   │   ├── deploy.go
│   │   └── snapshot.go
│   ├── docker/          # Docker 业务逻辑
│   │   ├── service.go   # Service 构造函数
│   │   ├── container.go
│   │   ├── image.go
│   │   ├── network.go
│   │   └── registry.go
│   ├── swarm/           # Swarm 业务逻辑
│   │   └── service.go
│   └── system/          # 系统业务逻辑
│       ├── member.go
│       ├── settings.go
│       └── stat.go
├── helper/          # 跨包通用工具函数
│   ├── response.go      # API 响应封装（RespondSuccess、RespondError）
│   └── websocket.go     # WebSocket 升级器
├── registry/        # 服务注册中心（统一管理外部服务连接）
│   ├── registry.go      # Registry 入口（Init 函数）
│   ├── docker.go        # Docker/Swarm 服务管理
│   ├── apisix.go        # Apisix 服务管理
│   └── compose.go       # Compose 服务管理
└── server/          # 应用入口与轻量 handler
    ├── app.go           # 路由注册与应用启动（NewApp 函数）
    ├── auth.go          # 认证中间件（AuthMiddleware）
    ├── handler_agent.go
    ├── handler_apisix.go
    ├── handler_auth.go
    ├── handler_compose.go
    ├── handler_docker.go
    ├── handler_filer.go
    ├── handler_shell.go
    ├── handler_swarm.go
    └── handler_system.go

config/             # 配置加载与类型定义
├── load.go             # 配置加载逻辑与环境变量处理
└── types.go            # 配置结构体定义

pkgs/
├── apisix/          # Apisix 原生客户端（返回原生类型）
│   ├── client.go        # HTTP 客户端
│   ├── route.go         # 路由管理
│   ├── consumer.go      # Consumer 管理
│   ├── upstream.go      # Upstream 管理
│   ├── plugin.go        # 插件管理
│   └── plugin_config.go # Plugin Config 管理
├── archive/         # 压缩/解压服务
│   └── zip.go
├── docker/          # Docker 原生客户端（返回原生类型）
│   ├── client.go        # Docker 客户端 + DockerConfig 注入
│   ├── container.go     # 容器管理
│   ├── image.go         # 镜像管理
│   ├── network.go       # 网络管理
│   ├── volume.go        # 卷管理
│   ├── stats.go         # 容器统计
│   ├── compose.go       # Compose 文件操作
│   ├── exec.go          # 容器终端
│   ├── registry.go      # 镜像仓库操作
│   └── helper.go        # 包内私有工具
└── swarm/           # Swarm 原生客户端（返回原生类型）
    ├── client.go        # Swarm 管理器 + 构造函数
    ├── node.go          # 节点管理
    ├── service.go       # 服务管理
    └── task.go          # 任务管理
```

**pkgs 内部引用规则**：pkgs 内部子包之间可以互相引用（如 `swarm` 引用 `docker` 的 `ParseDockerLogs`），但需保持合理的依赖方向，避免循环依赖。auth 逻辑简单，直接在 handler 内实现，不放 pkgs/。

### 安全规范

- 文件路径操作统一使用 `getAbsolutePath`（filer 包内私有）防止目录遍历攻击
- 压缩文件解压统一进行 Zip Slip 防护（检查解压路径不超出目标目录）
- WebSocket 连接统一通过认证中间件校验 token
- 文件权限修改统一解析八进制模式并校验有效性
- 禁止在代码中硬编码密码或密钥
- 敏感配置（JWT、Admin Key 等）后端 **禁止向前端回流明文**，仅返回 `xxxSet` 布尔标志位
- 关键资源保护：第一个系统账号、内置角色等 **前后端双重校验**，前端按钮置灰 + 后端返回 400/403

### Git 规范

#### 提交信息格式

```
<type>: <subject>

<body>
```

type 可选值：

| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | 修复 bug |
| refactor | 重构（不改变功能） |
| style | 格式调整（不影响逻辑） |
| docs | 文档更新 |
| chore | 构建或辅助工具变更 |

#### 分支规范

- `main`：生产分支
- `dev`：开发分支
- `feature/<name>`：功能分支
- `fix/<name>`：修复分支

### 样式一致性检查要求

所有页面必须定期进行样式一致性检查，确保：

1. **表格表头样式统一**：所有模块的表格表头必须使用 `text-xs font-semibold text-slate-600 uppercase tracking-wider`
2. **卡片标题栏统一**：所有页面的卡片标题栏必须使用 `bg-slate-50 border-b border-slate-200 rounded-t-2xl px-4 md:px-6 py-3`
3. **移动端布局统一**：所有列表页必须正确实现 `md:hidden space-y-3 p-4` 移动端卡片布局
4. **按钮颜色语义统一**：所有操作按钮必须遵循统一的颜色语义系统
5. **按钮类名统一**：所有按钮必须使用 `btn-icon` 类名
6. **按钮文本一致性**：按钮文本必须与功能正确对应，避免语义混淆

### 定期检查清单

- [ ] 检查所有表格表头样式一致性
- [ ] 检查所有卡片标题栏样式一致性  
- [ ] 检查移动端卡片布局实现情况
- [ ] 验证按钮颜色语义系统正确性
- [ ] 确认按钮类名统一性
- [ ] 核实按钮文本与功能对应关系

通过定期执行上述检查，确保项目前端样式保持高度一致性和规范性。
